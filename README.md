# gitignorer
Generate .gitignore automatically.



## How-To

```bash
go build -o gitignorer gitignorer.go
mv gitignorer /usr/local/bin
chmod +x /usr/local/bin/gitignorer

# under your project path
gitignorer
```

